#include <stdio.h>

int main(){
    
    int n = 23;
    for(int i = 0; i < n; i++)
    {
       printf("The value of i is %d\n", i); 
       /* After the checking of condition means whether the condition for i less than n is satisfied or not till
        then the for loop continues and then this block of code is executed and then the value of i is updated 
        according to increment or decrement. */
    }
    
    return 0;
}