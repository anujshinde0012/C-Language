#include <stdio.h>
#include <math.h> // We will now use this library function whenever we will solve any maths problems.

int main(){
    int a = 4;
    printf("The area of square whose side a is %f\n", pow(a, 2)); 
    // Pow is a function here which represents a to the power 2.
    // Also in place of %d, %f is used because the answer given to us will be given in decimal form.//
    
    return 0;
}