#include <stdio.h>

int main(){
    
     // ADDITION OF A NUMBER TO A POINTER :
     // int arr[6] = {7, 8, 9, 6, 3, 1};
     // int *ptr = arr;
     // printf("The initial point was %d\n", *ptr);
     // ptr = ptr + 3;
     // printf("The new value of ptr is %d\n", *ptr);
     // return 0;

     // SUBTRACTION OF A NUMBER FROM A POINTER :
     // int arr[] = {1, 2, 3, 4};
     // int *ptr = &arr[4];
     // printf("The initial point is %d\n", *ptr);
     // ptr = ptr - 2;
     // printf("The new value of ptr is %d\n", *ptr);

     // SUBTRACTION OF ONE POINTER FROM ANOTHER :
     // int arr[] = {10, 20, 30, 40, 50};
     // int *ptr1 = &arr[4];
     // int *ptr2= &arr[1];

     //int difference = ptr1 - ptr2;
     // printf("Address stored in ptr1: %p\n", (void *)ptr1);
     //printf("Address stored in ptr2: %p\n", (void *)ptr2);
     // printf("Difference between ptr1 and ptr2: %d elements\n", difference);

     // COMPARISON OF TWO POINTER VARIABLES : 
     // int arr[] = {10, 20, 30, 40, 50}; 
     // int *ptr1 = &arr[1];  
     // int *ptr2 = &arr[4];  

     // if (ptr1 == ptr2) 
     // {
     //   printf("ptr1 and ptr2 point to the same memory location.\n");
     // } 
     // else if (ptr1 < ptr2) 
     // {
     //   printf("ptr1 points to a location before ptr2.\n");
     // } 
     // else {
     //   printf("ptr1 points to a location after ptr2.\n");
     // }
}